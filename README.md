# backupgo

backup your data 2 oss, and support tg/mail notice.

check config first: `config/config.yml`，example at `config/config.example.yml`

Start:

``` shell
cp script_example/rebuild.sh rebuild.sh
chmod +x rebuild.sh
./rebuild.sh
```